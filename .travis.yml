sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t bolian/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true bolian/react-test npm test

after_success:
  - docker build -t bolian/multi-client ./client
  - docker build -t bolian/multi-nginx ./nginx
  - docker build -t bolian/multi-server ./server
  - docker build -t bolian/multi-worker ./worker